[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"

[[deploy.volumes]]
mountPath = "/data"

[variables]
PORT = "8080"
HL_TESTNET = "true"
WOLF_PRESET = "default"
RUN_MODE = "wolf"
INSTRUMENT = "ETH-PERP"
DATA_DIR = "/data"
